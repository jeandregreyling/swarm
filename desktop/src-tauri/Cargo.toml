[package]
name = "sevens-swarm"
version = "4.0.0"
description = "Seven's Swarm — Desktop wrapper"
authors = ["Seven"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
